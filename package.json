{
  "name": "apex-drive-pro",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "deploy:prepare": "npm run build && node scripts/prepare-deploy.mjs",
    "start": "npm run deploy:prepare && node scripts/serve-upload.mjs",
    "serve:upload": "node scripts/serve-upload.mjs",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "cannon-es": "^0.20.0",
    "howler": "^2.2.4",
    "lil-gui": "^0.21.0",
    "three": "^0.165.0",
    "vite": "^5.4.0"
  }
}
